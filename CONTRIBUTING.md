# Contributing Guidelines 🇺🇦

Дякуємо, що допомагаєте робити цей календар точнішим та кориснішим 💛💙

Це спільнотний проєкт. Ми приймаємо зміни, які:
- уточнюють або виправляють дати;
- додають нові **офіційні** професійні дні;
- додають **дні памʼяті та вшанування**;
- виправляють технічні помилки в ICS-файлах.

Будь ласка, дотримуйтесь цих правил перед створенням Pull Request.

---

## 1. Що ми додаємо

✅ Додаємо:

- офіційно встановлені професійні дні:
  - Збройні Сили України;
  - інші військові та силові структури (НГУ, НПУ, ДПСУ, ДСНС, розвідка тощо);
- дні памʼяті, вшанування та ключові символічні дати, повʼязані із захисниками України;
- зміни, які підтверджуються:
  - законами України;
  - указами Президента України;
  - постановами органів влади;
  - офіційними публікаціями державних інституцій;
  - надійними медіа з посиланням на офіційні джерела.

❌ Не додаємо:

- комерційні, маркетингові чи брендовані «свята»;
- мемні або випадкові дати без офіційного статусу;
- події, не повʼязані із захисниками України або сектором безпеки й оборони.

---

## 2. Структура календарів

У директорії `calendars/` є три основні файли:

1. `ua-military-full.ics`  
   Повний календар: **усі** офіційні професійні дні та дні памʼяті.

2. `ua-military-official.ics`  
   Лише **офіційні професійні дні** силових структур.

3. `ua-military-memory.ics`  
   Лише **дні памʼяті та вшанування** (Крути, Небесна Сотня, полеглі захисники тощо).

Правило узгодженості:

- `ua-military-full.ics` = вміст `ua-military-official.ics` + вміст `ua-military-memory.ics`.
- Якщо додаєте **офіційний** день:
  - оновіть `ua-military-official.ics`;
  - оновіть `ua-military-full.ics`.
- Якщо додаєте **день памʼяті**:
  - оновіть `ua-military-memory.ics`;
  - оновіть `ua-military-full.ics`.

---

## 3. Формат подій (ICS)

Кожна подія має вигляд:

    BEGIN:VEVENT
    UID:ua-<scope>-<short-id>@ua-mil
    SUMMARY:Коротка назва українською
    DTSTART;VALUE=DATE:YYYYMMDD
    RRULE:FREQ=YEARLY;BYMONTH=M;BYMONTHDAY=DD
    END:VEVENT

Правила:

- `scope`:
  - `full` — подія у `ua-military-full.ics`;
  - `off` — подія у `ua-military-official.ics`;
  - `mem` — подія у `ua-military-memory.ics`.
- `short-id`:
  - тільки латиниця та дефіси, без пробілів (напр.: `afu-day`, `kruty`, `marines`).
- `UID` має бути **унікальним** у межах кожного файлу.
- `SUMMARY`:
  - українською,
  - без емодзі,
  - чітко: напр. `День Збройних Сил України`.

### RRULE

Для фіксованих дат:

    RRULE:FREQ=YEARLY;BYMONTH=<M>;BYMONTHDAY=<DD>

Для подій типу «перша неділя місяця»:

    RRULE:FREQ=YEARLY;BYMONTH=<M>;BYDAY=SU;BYSETPOS=1

Для «остання субота місяця»:

    RRULE:FREQ=YEARLY;BYMONTH=<M>;BYDAY=SA;BYSETPOS=-1

`DTSTART;VALUE=DATE:YYYYMMDD`:
- використовує реальну дату в форматі `YYYYMMDD`;
- рік може бути роком установлення дати або поточним — повторення задається через `RRULE`.

---

## 4. Як створити Pull Request

1. Зробіть **fork** репозиторію.
2. Відредагуйте відповідні `.ics` файли в `calendars/`:
   - дотримуйтесь формату `VEVENT`;
   - використовуйте унікальні `UID`;
   - не забудьте оновити всі повʼязані файли (`full`, `official`, `memory`), якщо це потрібно.
3. У описі PR вкажіть:
   - які події додано або змінено;
   - посилання на **офіційні джерела** для кожної нової/зміненої дати.
4. Створіть PR до гілки `main`.

---

## 5. Автоматична валідація

У репозиторії налаштований GitHub Actions workflow:
`.github/workflows/validate-ics.yml`

Він:
- запускається на кожен `push` і `pull_request`;
- перевіряє всі `*.ics` файли за допомогою Python-пакету `icalendar`;
- якщо файл некоректний, перевірка падає і виводить повідомлення з помилкою.

Перед merge переконайтеся, що всі перевірки пройдені ✅

---

## 6. Мови

- Pull Requests приймаються українською або англійською.
- Усі `SUMMARY` в ICS — українською, для узгодженості з офіційною термінологією.
- Обговорення англійською вітається для зручності міжнародної спільноти.

Дякуємо за внесок і за те, що допомагаєте системно вшановувати наших захисників 🙏
